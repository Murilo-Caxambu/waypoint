# waypoint
Waypoint is an IoT smart compass that bridges the gap between digital maps and physical orientation. Instead of pointing North, it physically points to a destination selected by the user via a web interface.
